name: kvs
version: "0.1.0"
author: Shane M. <shanemoloneybusiness@gmail.com>
about: key store db
args:
#  - set:
#      short: s
#      long: set
#      value_names: [KEY, VALUE]
#      help: Sets key value pair
#      takes_value: true
  - set:
      help: Sets the input file to use
      required: false
      takes_value: true
      multiple: true
  - get:
      help: Sets the input file to use
      multiple: true
      last: true
      required: false
  - version:
      short: v
      help: tells the version number
subcommands:
  - KEY:
      about: not sure
      author: shane  <shane@other.com>
      args:
        - KEY:
            help: Sets the input file to use
            required: false
        - VALUE:
            help: Sets the input file to use
            required: false
